= form_for @paycheck do |f|
  - if @paycheck.errors.any?
    .alert.alert-danger{ role: 'alert' }
      %ul.errors
        - @paycheck.errors.full_messages.each do |msg|
          %li= msg

  .panel.panel-default
    .panel-body
      .form-group
        = f.label :name, class: 'control-label'
        = f.text_field :name, class: 'form-control', autofocus: true

      .form-group
        = f.label :next_pay_date, class: 'control-label'
        = f.date_select :pay_date, { discard_year: true }, { class: 'form-control' }

      .form-group
        = link_to 'Cancel', root_path, class: 'btn btn-default cancel'
        = f.submit 'Add', class: 'btn btn-primary'
