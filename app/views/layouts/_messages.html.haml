-# Rails flash messages styled for Bootstrap 3.0
- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "alert alert-#{name.to_s == 'notice' ? 'success' : 'warning'}"}
      %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} &times;
      = content_tag :div, msg, :id => "flash_#{name}"
.alert.alert-info
  Thanks for joining us!
  %br
  We are just getting things going here, but will soon start charging for the service. Want to get 1 year free when we do? Post on Instagram with the hashtag #billsandstuff about how much you like the product and send us the link at support@billsandstuff.com.
